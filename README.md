## Executando os testes

O projeto utiliza Poetry para gerenciamento das dependências e Pytest para execução dos testes.

### Instalar as dependências

Na raiz do projeto, execute:

```bash
poetry -C backend install
